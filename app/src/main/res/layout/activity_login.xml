<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="20dp"
    tools:context="com.yo.android.chat.ui.fragments.OTPFragment">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:src="@drawable/app_logo" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="40dp"
        android:text="An SMS with PIN would be sent to this number for verification"
        android:textColor="@android:color/white" />

    <org.angmarch.views.NiceSpinner xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/spCountrySpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:textTint="@color/black" />

    <EditText
        android:id="@+id/et_enter_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="20dp"
        android:background="@android:color/white"
        android:ems="10"
        android:hint="Enter mobile number"
        android:imeOptions="actionNext"
        android:inputType="number"
        android:padding="13dp"
        android:singleLine="true" />

    <EditText
        android:id="@+id/et_re_enter_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:background="@android:color/white"
        android:ems="10"
        android:hint="Re-enter mobile number"
        android:imeOptions="actionDone"
        android:inputType="number"
        android:padding="13dp"
        android:singleLine="true" />

    <Button
        android:id="@+id/email_sign_in_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/resend_bcgrnd"
        android:text="@string/next"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</LinearLayout>
