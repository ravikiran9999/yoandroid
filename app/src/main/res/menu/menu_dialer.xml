<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search_white_36dp"
        android:title="Search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="always|collapseActionView" />
    <!--<item
        android:id="@+id/notification_icon"
        android:title="@string/notifications"
        android:visible="false"
        app:showAsAction="always" />-->
    <item
        android:id="@+id/menu_clear_history"
        android:title="@string/clear_call_history"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_all_calls"
        android:title="@string/all_calls"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_app_calls"
        android:title="@string/free_calls"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_paid_calls"
        android:title="@string/paid_calls"
        app:showAsAction="never" />

</menu>